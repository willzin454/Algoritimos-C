#include <stdio.h>

int main ()
{
	int i;
	for (i=1;i<=20;i++(ou se se quiser ir de 2 em 2 ou 3..., é i=i+2))
		printf ("Numero: %d\n", i);
return 0;
}
------------------------------------------------
#include <stdio.h>

int main ()
{
	int n = 0;
	while (n>=0)
	{
		printf("Digite um numero: ");
		scanf("%d", &n);
	}
	return 0;
}
------------------------------------------------
#include <stdio.h>

int main() 
{
	int n;
	do {
		printf("Digite um numero: ");
		scanf("%d", &n);
		printf("Numero: %d\n", n);
		} while(n>=0);
	return 0;
}
